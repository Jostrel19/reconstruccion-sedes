# HANDOFF — 2026-09-24

Cierre del día a pedido del usuario: **todo lo de abajo queda pendiente de revisar**; se decide si se
hace o no cuando se retome. Nada está commiteado (no se pidió).

## Estado al cierre

- **Desplegado y probado en real hoy:** backend D-44 completo (9 `.gs`, `crearHojas()` con 8 pestañas),
  transporte con reintentos, sesión que sobrevive a F5, lotes (crear, quitar, mover, cerrar), buscador.
  Detalle en `docs/REGISTRO_DESARROLLO.md`.
- **Código listo, probado sin desplegar** (arnés 54/54 + simulador con el JSON real de Samaná):
  - `Presupuestos.gs` — D-45 (un borrador no reemplaza un radicado; hallazgos solo al radicar) y
    `volcarCarga` (Cargas por tandas, idempotente, sin filas en $0).
  - `Codigo.gs` — registra `volcarCarga`.
  - `Sedes.gs` — `archivo_origen` en el resumen de cada sede.
  - `mockup_v6.html` — Cargas corregida (6 defectos: tildes del municipio, un pedido por sede, filas en
    $0 premarcadas, casillas corridas, «ya tiene vN» desactualizado, catálogo no cargado al subir el
    JSON), Registrar/Ficha con borrador en espera.
- **JSON de Cargas** regenerados con los lectores reales: `data/generado/ingesta_{SAMANA,ARANZAZU,AGUADAS,BELALCAZAR}.json`.

## Pendiente de revisar y decidir

1. **Desplegar** `Presupuestos.gs`, `Codigo.gs` y `Sedes.gs` (nueva versión de la implementación
   existente). Descripción sugerida: «Cargas por tandas (volcarCarga, idempotente, sin filas en $0);
   D-45: un borrador no reemplaza un radicado; archivo_origen en el resumen de sedes».
2. **Correr `docs/GUION_CASO_DE_EXITO.md` en real**, una vez y en orden (caso de uso de éxito y prueba
   de aceptación). Necesita: código de ingreso y el correo del Responsable de sede de prueba (propuesto:
   el correo personal del practicante, alcance Aranzazu, se desactiva al terminar).
3. **Plan propuesto para salir del estancamiento** (no aprobado todavía): cuatro hitos con condición de
   salida — (1) demostración estable con el guion en verde, (2) presentación al jefe con lista cerrada
   de decisiones (Q-3, Q-7, Q-14, municipio piloto, publicar y borrar datos de prueba), (3) piloto real de
   2 semanas con 1 alcaldía y 1 arquitecto, (4) módulo de obra con los campos que defina el jefe. Regla:
   nada nuevo hasta terminar el piloto. Si se aprueba, pasa a `docs/PLAN_DESARROLLO.md`.
4. **Pregunta abierta:** ¿fecha de presentación al jefe?
5. **Seguimiento de obra (Q-14):** construir ya o dejar como siguiente fase; requiere definir campos.
6. Pendientes menores ya anotados: Aguadas `217013000602` quedó con un borrador de $0 vigente (dato de
   prueba creado antes de D-45); texto obsoleto al final de `Setup.gs::crearHojas`; tras quitar o mover
   una sede de un lote la pantalla tarda ~20 s (recarga las 975); San José mandó Excel sin lector.
7. Pruebas en real que quedaron sin hacer: correo al radicar, foto de celular, corte de sesión de un
   usuario desactivado, CSV, línea de tiempo, recorrido de Verificación/Hallazgos/Usuarios.
8. Mejoras propuestas antes (sin aprobar): correo al emitir concepto, lista de tareas en Registrar,
   precargar el detalle de la Ficha, revisión WCAG completa y lineamientos GOV.CO.
